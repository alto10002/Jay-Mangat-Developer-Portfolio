name: Keep Render Server Awake

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Curl ping to Render server
        run: curl -sSf https://jay-mangat.vercel.app/
